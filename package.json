{
  "name": "restful-booker-api-automation",
  "version": "1.0.0",
  "description": "Production-ready API automation framework for Restful Booker using Playwright and TypeScript",
  "private": true,
  "type": "commonjs",
  "scripts": {
    "test": "playwright test",
    "test:api": "playwright test tests/api",
    "test:auth": "playwright test tests/api/auth",
    "test:booking": "playwright test tests/api/booking",
    "test:report": "playwright show-report",
    "type-check": "tsc --noEmit",
    "lint": "eslint .",
    "format": "prettier . --write",
    "format:check": "prettier . --check",
    "check": "npm run type-check && npm run lint && npm run format:check && npm run test:api",
    "security:audit": "npm audit --audit-level=high",
    "allure:generate": "allure generate allure-results --clean -o allure-report",
    "allure:open": "allure open allure-report",
    "allure:serve": "allure serve allure-results",
    "allure:clean": "rm -rf allure-results allure-report",
    "test:api:allure": "npm run allure:clean && playwright test tests/api",
    "report:allure": "npm run test:api:allure && npm run allure:generate && npm run allure:open"
  },
  "dependencies": {
    "dotenv": "17.4.2"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "@playwright/test": "1.60.0",
    "@types/node": "25.9.2",
    "@typescript-eslint/eslint-plugin": "^8.60.1",
    "@typescript-eslint/parser": "^8.60.1",
    "allure-commandline": "2.42.0",
    "allure-js-commons": "3.9.0",
    "allure-playwright": "3.9.0",
    "eslint": "^10.4.1",
    "eslint-plugin-playwright": "^2.10.4",
    "globals": "^17.6.0",
    "prettier": "3.8.3",
    "typescript": "6.0.3",
    "typescript-eslint": "^8.60.1",
    "zod": "4.4.3"
  }
}
